import { create } from "zustand";

//local state from formbuilder should be switched to global
//templates modal will also populate this state on template edit

const useFormBuilderStore = create(() => {

})

export default useFormBuilderStore